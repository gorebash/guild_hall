<% content_for :title do %>
  <h1>Upcoming Merry Feasts</h1>
<% end %>

<div id="guild_events">
  <div class="container">
    <div class="row">
      
        <% @guild_events.each do |guild_event| %>
          <%= render guild_event %>
        <% end %>
      
    </div>
  </div>
</div>

<%= link_to "New guild event", new_guild_event_path %>
